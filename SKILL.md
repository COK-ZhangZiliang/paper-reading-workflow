---
name: paper-reading-workflow
description: 将单篇技术论文或技术报告整理为有来源依据的结构化笔记。完整精读优先从 Zotero 取文，并默认更新对应 Notion 数据库中的匹配条目；没有匹配条目时在该数据库中新建，随后回读验证。用户要求仅回答或本地草稿时不写入 Notion；单项技术追问默认不写入。适用于单篇精读、技术追问和既有笔记修订；不用于宽泛文献检索、论文写作或同行评审。
---

# 论文阅读工作流

把一篇论文或技术报告转化为可追溯、可继续追问的阅读产物，并让后续解释与修订始终锚定同一份原始来源。遵循用户指定的语言与深度；未指定时使用清晰中文，英文术语首次出现时保留原文。

## 请求路由

- **精读论文、技术报告或起草完整笔记：**读取 [references/full-note-and-evidence.md](references/full-note-and-evidence.md) 和 [references/live-publication.md](references/live-publication.md)，默认完成 Zotero 到对应 Notion 数据库条目的发布与回读验证；没有匹配条目时在该数据库中新建。
- **用户明确要求仅回答、只生成草稿、本地保存或不要写入 Notion：**只读取完整笔记参考，不查找或修改 Notion；文献来源仍从用户提供的材料或 Zotero 按需取得。
- **解释公式、图表、指标、术语或实现细节：**读取 [references/technical-explanations.md](references/technical-explanations.md)；单项技术追问默认只回答，除非用户同时要求修订笔记。
- **修订技术小节：**读取技术解释参考；若要修改在线页面，再读取在线发布参考。

完整笔记任务默认识别文献来源、对应 Notion 数据库和匹配条目；用户明确选择仅回答或本地草稿时，只识别文献来源。单项技术追问也只识别文献来源，除非用户要求写回笔记。对应数据库或匹配条目仍存在多个合理候选且选错会导致错误写入时，再向用户确认。

## 共同约束

1. 确认文献的准确身份、版本和来源。优先使用原始正文、补充材料与官方实现，不以二手总结代替；容易变化的元数据或实现细节须回到一手来源复核。
2. 阅读命中位置的上下文，而不是只看搜索片段或截图。公式、表头、坐标轴、图例或图注可能被文本抽取打乱时，必须渲染并检查对应 PDF 页面。
3. 描述性文字应清晰、精炼，删除重复、空泛和不推动理解的表述，但不得以简化为由省略理解问题、方法或结论所必需的信息。核心方法与公式必须展开解释：先说明直觉、作用与执行流程，再解释关键符号、假设和适用边界，避免从公式直接跳到结论。
4. 只生成用户明确要求的内容部分。结构优先级为：用户明确指定的结构；技术报告自身的章节；普通论文的默认结构。材料本身明确为技术报告且用户未另行指定时，顶部保留 `TL;DR`，其后以报告正文的顶层章节及原有顺序组织笔记，不强行套用固定的五个论文章节。其他完整论文笔记在用户未指定结构时，顶层正文严格使用 `TL;DR`、`Background`、`Motivation`、`Method`、`Experiments`、`Drawbacks`。两种场景都不自行增加论文信息表、目录、参考资料或附录等模块；章内可按内容需要使用小标题或列表。来源核对在内部完成，必要限定应写进相关章节。
5. 除章节标题外，笔记正文的段落、列表、表格和 callout 默认都不使用 Markdown、HTML 或 Notion 加粗；术语、结论、数值和句首标签也不靠粗体强调。只有用户明确要求时才使用加粗。
6. 在推理中区分以下四类证据，并在产出中通过自然措辞保留归属，不输出固定标签、清单或独立模块：
   - 原文定义或作者主张；
   - 实验直接展示的结果；
   - 阅读者综合或推断；
   - 尚未验证的内容。
7. 不扩大结论范围。代理指标、松弛形式、采样统计、单一配置或解析估算，不得被表述成精确等价、通用阈值、因果证明或端到端实测。
8. 优先沿用原文术语。对容易混淆的量，明确其定义、所指对象、时间或版本、作用阶段与粒度；只有比值才需要说明分子和分母。不要合并解释同名或名称相近但定义不同的对象。
9. 将与当前论点相关的图表和正文共同作为证据来源。先核对图注、坐标轴或表头、图例和相邻正文，再把图表直接支持的比较、趋势、数值和边界自然融入它所支持的现有章节或段落，并沿用周围格式。除非用户明确要求逐图分析，不另设“图表解读”模块，不按 Figure 制作卡片、callout 或逐图清单，也不使用过程标签；图号可以定位来源，但不能代替具体内容。除非用户明确要求截图、插图、上传或嵌入，否则不创建图片块。
10. 把交付状态也视为证据：`已起草`、`已排队`、`已写入`、`回读验证通过` 是不同结果。仅有草稿、请求受理或异步任务成功，都不能宣称已经发布。

## 完成标准

交付用户要求的笔记、解释或修订。不要为汇报来源或证据边界而给笔记增加用户未要求的章节或区块；必要限定应融入相关内容。完整笔记默认只有在更新匹配条目，或在没有匹配条目时于对应 Notion 数据库中新建条目，并完成写入与回读后才算完成。用户明确选择仅回答或本地草稿时，以聊天回复或用户指定的本地文件为完成结果。单项技术追问不自动写回。若来源、数据库、权限或连接器阻断默认发布，应保留有用草稿，并准确说明哪个步骤仍未验证。
